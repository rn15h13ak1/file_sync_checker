# Changelog

[Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) 形式に概ね準拠し、
[セマンティックバージョニング](https://semver.org/lang/ja/) に従います。

バージョンはコミット履歴からさかのぼって付けたもので、タグは打っていません。

## [Unreleased]

### Added
- `CHANGELOG.md`（本ファイル）。コミット履歴からさかのぼって 4 つの版に整理した
- `CLAUDE.md` に本リポジトリ固有の事情を書いた。共通規約からの逸脱（commit / push と
  テストを確認なしで行う）、コミット前に実行する検査、配布済み設定を壊さないこと、
  Windows 前提の制約の 4 つ
- `tests/test_environment.py`。動作要件どおりの環境で起動できることを固定する。
  **開発機は Python 3.12 以降で依存も入っているため、ここが壊れても他のテストは
  全部通る。** 注釈は AST で走査し、依存が欠けた状態は `sitecustomize` で
  `import` を名前ごと撥ねて再現する（アンインストールを伴わない）

### Fixed
- **Python 3.9 でメニューが起動しなかった。** `menu.py` が `str | None` という
  Python 3.10 以降の注釈を `from __future__ import annotations` なしで使っており、
  関数を定義した時点で `TypeError` になる。動作要件には 3.9 以上と書いてあり、
  配布先の Windows で同梱の Python を使うとは限らない。9 箇所が該当した
- **依存が入っていない Python で `main.py` を起動すると、終了コード 1 を返していた。**
  1 は「差分を検出」で、定期実行の通知では正常な検出と区別が付かず、**レポートが
  一枚も出ていないことに気付けない。** `menu.py` には対処を表示する仕組みがあったが、
  `main.py` には無く、トレースバックのまま終了していた。導入コマンドと実行中の
  インタプリタのパスを表示して `2` で止まるようにした（Windows には py ランチャ経由の
  Python が複数入っていることがあり、「pip install したのに直らない」が起こりやすい）

### Changed
- 終了コード `2` の意味を「設定エラー」から「設定エラー、または依存ライブラリが
  入っていない」に広げた。返す条件が増えただけで、従来 `2` だった場合の挙動は変わらない
- `~/ws` 配下の共通規約（`ws-conventions`）に合わせた。`.gitignore` を定型に揃え、
  `.claude/` と資格情報のパターンを追加した
- テストの固定値から絶対パスらしき文字列を外した（`/tmp/dummy` → `/mnt/dummy`、<!-- check-privacy:ignore -->
  `C:/Users/foo` → `C:/work/foo`）。<!-- check-privacy:ignore -->
  共通規約の個人情報検査が毎回 9 件を報告する状態が
  続くと、**「いつもの件数」として読み飛ばす習慣がつき、本物の混入を見落とす。**
  いずれも実在しない架空のパスで、テストの意味は変わらない。
  これで 3 つの検査がいずれも指摘 0 件になった

## [0.4.0] - 2026-09-16

対話メニューを用意し、比較する組を設定ファイルに複数書けるようにしました。

### Added
- 対話メニュー（`menu.py` / `menu.bat`）。CLI オプションを覚えなくても番号を選ぶだけで
  実行でき、Windows では `menu.bat` のダブルクリックから起動できる。操作は
  「チェックを実行」「条件を選んで実行」「設定を確認」「前回のレポートを開く」の 4 つ
- メニューの見出しに比較する拠点名を出す。`config.yaml` のコメントアウトを外し忘れて
  意図しない拠点を比較する事故に、実行前に気付けるようにするため。実行後は終了コードを
  意味付きで表示する（例:「終了コード: 1 (差分を検出)」）
- 比較する組を名前を付けて複数書ける `comparisons:`。実行時に `--comparison NAME` で選び、
  `--list-comparisons` で一覧を出す。組が 1 つでも複数でも書き方は同じで、変わるのは
  実行時に名前を指定するかどうかだけ
- レポートを比較ごとに分ける（`sync-check-契約書-*.html` と固定名 `sync-check-契約書.html`）。
  古いレポートの削除も比較ごとに数えるので、互いのレポートを消し合わない。実行条件にも
  比較名を記録する（どの比較のレポートか単体で分かる）
- メニューの「チェックを実行」「条件を選んで実行」で、実行する比較を選べるようにした。
  1 つ選ぶか「すべて実行」かを決められる。組が 1 つの設定（従来形式を含む）では選択を
  挟まず即実行する
- すべて実行では順に照合し、最後に比較ごとの結果をまとめて表示する。1 件失敗しても
  残りは実行し（後続を確認できないと困るため）、中断されたときだけ打ち切る。表示する
  終了コードは最も重いもので、数字の大小ではなく対処の必要度で並べる
- 従来形式（トップレベルの `locations:`）を守るテスト。配布済みの `config.yaml` を
  そのまま使い続けられることはこのツールの前提条件なので、実際に `main.py` と `menu.py` を
  プロセスとして起動し、レポート名・固定名リンク・実行条件・メニュー表示が変わらないことを
  明示的に固定した

### Changed
- README の節の並びを backlog_issue_cloner に合わせた（主要機能 / 目次 / クイックスタート /
  動作要件 / 使い方ガイド / リファレンス / 開発者向け）。見出しからは全角括弧と
  バックティックを外した。アンカーの生成規則がツールによって違い、リンク切れの原因になるため
- `config.example.yaml` と README を `comparisons:` 中心に書き直した。組の数で書き分けるのは
  扱いづらいため、定義の仕方を一本化している。`locations:` は「従来形式（引き続き動作）」
  として残し、動作の対応表を載せた
- コンソールに出るソースを CP932 で表現できる文字だけにした。`scanner.py` と `comparator.py` の
  コメントにあった em dash（U+2014）は CP932 に無いため U+2015 に置き換えた
  （`reporter.py` は出力先が HTML/Excel なので対象外）
- `menu.bat` は CRLF・ASCII のみとし、`.gitattributes` で改行を固定した

### Fixed
- README の Markdown が崩れていた箇所を是正した
- コードフェンスと `====` の罫線が隣接すると Backlog Wiki で崩れるため、間に空行を入れた

## [0.3.0] - 2026-08-29

レポート肥大・ネットワーク I/O・中断応答性を実測ベースで改善し、大きい共有や
定期実行で落ちない・取りこぼさないようにしました。

### Added
- スキャンの 2 フェーズ化。列挙（サイズ・更新日時のみ）とハッシュ計算を分離し、
  `os.walk` を `os.scandir` の直接利用に置き換えた。フェーズ 1 は拠点ごとに並列化し、
  フェーズ 2 は拠点をまたぐ単一プールで全拠点の帯域を同時に使う
- `hash_mode: smart`（既定は従来どおり `always`）。サイズ相違（内容相違が確定）と、
  全拠点でサイズ・更新日時が一致するファイルを読まずに済ませる。実測で 1,500 ファイル /
  1.5GB・差分 25 件のケースが読み取り 1,500 → 75 ファイル、0.70 秒 → 0.04 秒
  （検出結果は `always` と同一）
- 照合キー（`match_key`）。既定で NFC 正規化してから突き合わせる。Windows 共有と Linux の
  混在向けに `case_sensitive: false` も追加した（既定は従来どおり区別する）
- `--retry N` で、失敗したファイルだけ間隔を空けて再試行する（0.5 秒 → 1.0 秒と倍加）。
  SMB 共有ではネットワークの瞬断や Office による一時的なロックですぐ解消する失敗が起こり、
  1 件でも失敗すると終了コード 3 になるため。既定は 0 で従来どおりの挙動
- `--format` / `-o` / `--hash-mode` による設定の上書き。CI から実行するときに
  `config.yaml` を書き換えずに済む
- `output.max_table_rows`（既定 20000、0 で無制限）。50,000 行規模の HTML レポートは
  DOM 75 万ノードでレンダラが 30 秒以上応答せず、ブラウザで開けなかった。実測で
  56.0MB / 30 秒以上無応答 → 22.4MB / ロード 1.06 秒になった。上限は HTML のみで、
  全件を確認する手段として Excel には全件を出す
- `output.keep_reports`（既定 0 = 削除しない）。大きい共有だと 1 回あたり HTML 22MB +
  Excel 3MB で、毎日実行すれば年間 9GB 規模になる。対象は
  `sync-check-YYYYMMDD-HHMMSS.{html,xlsx}` に完全一致する通常ファイルだけで、固定名の
  `sync-check.html` や無関係なファイルには触れない
- HTML の各表にパス絞り込みと状態絞り込みを追加し、表示件数を出す。50,000 行の
  レポートでは目的の行を探す手段が無く、ブラウザの検索も実用的でなかったため
- レポートのサマリーに実行条件（ハッシュ方式・省略件数・更新日時の許容誤差・
  除外パターン・ファイル名の照合設定）を記録する。読み手がその結果の意味する保証の
  程度を判断できるようにするため。CLI で上書きした場合も実際に使われた値が載る
- 列挙フェーズの進捗表示。走査したエントリ数を 500 件ごとに、拠点をまたいで 1 本の
  カウンタに集約して報告する。SMB 共有では 1 ファイルごとに往復が発生するため、
  大きい共有では数分間なにも表示されない状態になり得た
- 読み取りエラーに専用の終了コード 3、予期しない例外に 4 を割り当てた。どちらも 1 で、
  CI からは「差分が出ただけ」に見えていたため

### Changed
- HTML レポートを 6.7MB → 1.22MB に縮めた（1,000 ファイル × 3 拠点）。行ごとに詳細 HTML を
  data 属性へ埋め込んでいたため出力の 87% がその重複で占められていた。相対パスをキーにした
  JSON ブロック 1 つに集約し、詳細 DOM はクリック時に `textContent` で組み立てる。
  `innerHTML` を使わなくなり、パス名由来の HTML が解釈される余地も消えた
- Excel を write-only モードで書き出し、HTML はディスクへ流しながら生成するようにした。
  組み立て中に全体をメモリへ持たないため、行数が増えても使用量が伸びない
- 除外パターンを名前用・パス用の 2 本の正規表現に 1 回だけまとめる。エントリ × パターンの
  回数だけ `fnmatch` を呼んでおり、列挙フェーズの処理時間の 4 割を占めていた。実測で
  15 パターンの場合 222ms → 117ms
- 進捗バーを端末でないときは自動的に無効にする。cron から実行すると stderr を
  リダイレクトしていても tqdm が書き続けており、実測では 10 秒のスキャンで
  stderr 6,821 バイトのうち 97% が進捗バー由来の制御文字だった（→ 776 バイト）。
  `--no-progress` は明示的な指定として残す
- サマリーの「設定ファイル」「ルートパス」、エラー詳細のメッセージ、フォルダ構造差分の
  パスを省略せず折り返して全体を出す。行数が少ない表で、ファイル行と違ってモーダルも
  ツールチップも無かったため。サマリーのパスにはコピーボタンも付けた

### Fixed
- Ctrl+C が効くまで最大数秒かかっていた。`ThreadPoolExecutor` の `shutdown(wait=True)` が
  キュー済みタスクの完了を待つためで、中断フラグと `cancel_futures` を導入し、実行中の
  ハッシュもチャンク単位で降りるようにした。実測（1.5GB）で 2,483ms → 37ms
- macOS と Windows を混在させると、同じ日本語ファイル名が「欠落」と「余分なファイル」に
  二重計上されていた（濁点の分解形 NFD と合成済み NFC の違い）。正規化はキーにだけ使い、
  実ファイルを開く際は各拠点での元の名前を使う
- `"作業中/*"` のように `/` を含む除外パターンが 1 件も除外されなかった。ファイル名としか
  照合しておらず、エラーにも警告にもならないため書いた側は除外できたつもりになっていた
- 出力できないファイル名が 1 つあるとレポート全体を失っていた。制御文字は openpyxl が
  例外を投げ、不正な UTF-8 は HTML が書けないうえ Excel は開けない xlsx を作っていた
  （落ちるより悪い）。見える表記へ置換して続行する
- Excel の 1 シート 1,048,576 行を超えると保存時に失敗し、レポートが 1 枚も残らなかった。
  切り詰めて末尾に省略件数を注記する
- 固定名 `sync-check.html` を直接上書きしていたため、書き込み中に開くと途中までの HTML を
  読む可能性があった。一時ファイルに書いてから `os.replace` で差し替える
- README と実装の食い違いを直した（「エラー対象ファイル」「読み取り再試行」の記載漏れ、
  テストケース数）

## [0.2.0] - 2026-05-22

誤検出を潰し、レポートから拠点ごとのパスをそのままコピーできるようにしました。

### Added
- エラー対象ファイルの扱い。`ScanResult.file_errors` と `STATUS_ERROR` を追加し、
  ハッシュ計算に失敗したファイルを欠落・余分・不一致に混ぜず分離する。Excel のシートと
  HTML のセクション、コンソールの件数表示を追加した
- ファイル行をクリックするとモーダル（HTML5 `<dialog>`）で詳細を出す。拠点ごとに
  ファイルパス・フォルダ・SHA-256 のコピーボタンが使える。クリップボードは新旧 API の
  フォールバックを併用し、`file://` で開いた場合でも動く
- `//server/share` や `C:/...` は Windows と判定してセパレータを `\` に統一する。
  コピーした文字列はリンクではなくプレーンテキストとして入るため、エクスプローラへ
  そのまま貼って開ける
- タイムスタンプ無しの `sync-check.html` をあわせて出力する。ブックマークや自動化
  スクリプトが常に最新レポートを固定のパスから参照できる（Excel は対象外）
- 拠点パスの重複検出（末尾スラッシュ違いや Windows の case 違いも同一視する）
- pytest による自動テスト。MIT LICENSE を追加した

### Changed
- ファイル表で横スクロール時に固定する列を、相対パスからファイル名に変更した。
  深い階層のパスより、ファイル名のほうが行を特定しやすい場面が多いため
- 欠落セルのプレースホルダを全角「－」から半角 `-` に変更（環境依存の幅崩れを回避）
- `requirements.txt` にメジャーバージョン上限を付けた（PyYAML<7 / openpyxl<4 / tqdm<5）
- 拠点パスにバックスラッシュが含まれる場合はエラーにする（`//server/share/docs` 形式を強制）

### Fixed
- ハッシュ不一致の行で全拠点のセルが赤くなっていた。最頻値と異なるハッシュ集合だけを
  赤く塗る（同数の場合は全部）
- ハッシュ計算に失敗したファイルが「欠落」として誤検出されていた
- 横スクロール時に固定列が透明になり、後続セルとテキストが重なっていた
- 表の列見出しがページヘッダーと衝突して隠れていた
- アンカーリンクでセクション見出しが画面端に潜っていた

## [0.1.0] - 2026-05-14

最初の実装。

### Added
- 3 拠点以上の共有フォルダの内容を SHA-256 で突き合わせ、Excel / HTML レポートを
  生成するツール一式
- `scanner`: ThreadPoolExecutor による並列ハッシュ計算と tqdm の進捗表示
- `comparator`: ハッシュ不一致 / 欠落 / 一部のみ存在 / フォルダ差分の分類
- `reporter`: openpyxl による 7 シート構成の Excel と、CSS 付きの HTML
- 除外パターン（`~$*` / `*.tmp` / `.DS_Store` など）を YAML で設定できる
- ロック中・読み取り失敗はエラー扱いで継続し、レポートに記録する
- 設定ファイル内の相対パスは設定ファイルのあるディレクトリ起点で解決する。
  `-c` を省略した場合の `config.yaml` は「CWD → スクリプト同梱」の順で探す
  （別のディレクトリからフルパスで起動しても出力先がずれない）
